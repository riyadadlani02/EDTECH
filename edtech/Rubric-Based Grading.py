def rubric_based_grading(student_answers, rubric):
    
    scores = {}  # Store scores for each question
    for 
        scores[question] = calculate_score(answer, rubric[question])
    return scores
